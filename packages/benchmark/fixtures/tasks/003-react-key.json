{
  "id": "003-react-key",
  "name": "react list key",
  "category": "framework",
  "prompt": "Create a file called UserList.tsx — a React component that takes an array of {id, name} users and renders a <ul> with each user's name as <li>.",
  "evaluator": {
    "type": "pattern",
    "wrong_patterns": ["key=\\{index\\}", "key=\\{i\\}"],
    "correct_patterns": ["key=\\{[a-zA-Z][a-zA-Z0-9_.]*\\.id\\}", "key=\\{item\\.id\\}"]
  }
}
