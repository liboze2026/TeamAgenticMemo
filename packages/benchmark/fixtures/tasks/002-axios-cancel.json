{
  "id": "002-axios-cancel",
  "name": "axios cancel request",
  "category": "api_currency",
  "prompt": "Create a file called http-client.ts with a TypeScript function fetchWithCancel(url, signal) that uses axios to make a cancellable HTTP GET request.",
  "evaluator": {
    "type": "pattern",
    "wrong_patterns": ["CancelToken", "axios\\.Cancel"],
    "correct_patterns": ["AbortController", "AbortSignal", "signal:"]
  }
}
